name: snowflake-migration-demo

on:
  push:
    branches:
      - main
    paths:
      - 'deploy/**'
  workflow_dispatch:

jobs:
  deploy-snowflake-changes-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.8
        uses: actions/setup-python@v4
        with:
          python-version: '3.8'

      - name: Create connections.toml
        run: |
          cat <<EOF > connections.toml
          [snowflake]
          account = "${{ secrets.SF_ACCOUNT }}"
          user = "${{ secrets.SF_USERNAME }}"
          password = "${{ secrets.SF_PASSWORD }}"
          role = "${{ secrets.SF_ROLE }}"
          warehouse = "${{ secrets.SF_WAREHOUSE }}"  # Confirmed correct secret name
          database = "${{ secrets.SF_DATABASE }}"
          EOF

      - name: Debug - Show masked config
        run: |
          echo "Config file contents (masked):"
          grep -v "password" connections.toml

      - name: Install schemachange
        run: pip install schemachange

      - name: Run schemachange
        run: |
          schemachange deploy \
            -f "$GITHUB_WORKSPACE/deploy" \
            -c "${{ secrets.SF_DATABASE }}.SCHEMACHANGE.CHANGE_HISTORY" \
            --create-change-history-table
